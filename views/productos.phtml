<!-- Vista: Detalle de Producto -->
<main class="py-10">
<div class="container mx-auto px-4 max-w-5xl">

    <!-- Breadcrumb -->
    <nav class="text-sm text-gray-500 mb-8">
        <a href="<?= BASE_URL ?>/index.php" class="hover:text-black">Inicio</a>
        <span class="mx-2">›</span>
        <a href="<?= BASE_URL ?>/pages/productos.php" class="hover:text-black">Catálogo</a>
        <span class="mx-2">›</span>
        <span class="text-black font-medium"><?= htmlspecialchars($producto['nombre']) ?></span>
    </nav>

    <div class="grid grid-cols-1 md:grid-cols-2 gap-12">

        <!-- Imagen -->
        <div class="bg-gray-100 rounded-2xl overflow-hidden aspect-square">
            <img src="<?= imgUrl($producto['imagen']) ?>"
                 alt="<?= htmlspecialchars($producto['nombre']) ?>"
                 class="w-full h-full object-cover"
                 onerror="this.onerror=null;this.src='<?= BASE_URL ?>/assets/img/placeholder.svg'">
        </div>

        <!-- Info -->
        <div class="flex flex-col justify-center">
            <span class="text-sm text-gray-500 uppercase tracking-widest mb-2"><?= htmlspecialchars($producto['categoria']) ?></span>
            <h1 class="text-3xl font-bold text-black mb-4"><?= htmlspecialchars($producto['nombre']) ?></h1>
            <p class="text-4xl font-bold text-black mb-4"><?= formatCOP($producto['precio']) ?></p>
            <p class="text-sm text-gray-500 mb-8">Stock disponible: <span class="font-semibold text-black"><?= $producto['stock'] ?> unidades</span></p>

            <?php if ($producto['stock'] > 0): ?>
            <button onclick="addToCart(<?= $producto['id'] ?>, '<?= addslashes($producto['nombre']) ?>', <?= $producto['precio'] ?>, '<?= $producto['imagen'] ?>', '<?= $producto['categoria'] ?>', <?= $producto['stock'] ?>)"
                    class="w-full bg-black text-white py-4 rounded-xl text-lg font-semibold hover:bg-gray-800 transition-colors mb-4">
                Añadir al Carrito
            </button>
            <?php else: ?>
            <button disabled class="w-full bg-gray-300 text-gray-500 py-4 rounded-xl text-lg font-semibold cursor-not-allowed mb-4">
                Agotado
            </button>
            <?php endif; ?>

            <a href="<?= BASE_URL ?>/pages/productos.php"
               class="text-center text-sm text-gray-500 hover:text-black transition-colors">
                ← Volver al catálogo
            </a>
        </div>
    </div>
</div>
</main>
