<!-- Vista: Iniciar Sesión -->
<main class="min-h-screen flex items-center justify-center py-12 px-4 bg-gray-50">
<div class="max-w-md w-full">
<div class="bg-white rounded-2xl shadow-lg p-8">
    <div class="text-center mb-8">
        <h1 class="text-3xl font-bold text-black mb-2">Iniciar Sesión</h1>
        <p class="text-gray-600">Accede a tu cuenta de FutbolShop</p>
    </div>
    <?php if ($error): ?>
    <div class="bg-red-50 border border-red-200 text-red-700 px-4 py-3 rounded-lg mb-6">
        <p class="text-sm font-medium"><?= htmlspecialchars($error) ?></p>
    </div>
    <?php endif; ?>
    <form method="POST" action="" class="space-y-6">
        <div>
            <label class="block text-sm font-semibold text-black mb-2">Correo Electrónico</label>
            <input type="email" name="email" required value="<?= htmlspecialchars($_POST['email'] ?? '') ?>"
                   placeholder="tu@email.com"
                   class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:outline-none focus:border-black transition-colors">
        </div>
        <div>
            <label class="block text-sm font-semibold text-black mb-2">Contraseña</label>
            <input type="password" name="password" required placeholder="••••••••"
                   class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:outline-none focus:border-black transition-colors">
        </div>
        <button type="submit"
                class="w-full bg-black text-white py-3 rounded-lg font-semibold hover:bg-gray-800 transition-colors">
            Iniciar Sesión
        </button>
    </form>
    <div class="mt-4 p-3 bg-gray-50 rounded-lg text-xs text-gray-500">
        <p class="font-semibold mb-1 text-gray-700">Credenciales de prueba:</p>
        <p>Admin: admin@futbolshop.com / Admin123</p>
        <p>Usuario: usuario@futbolshop.com / Usuario123</p>
    </div>
    <div class="mt-4 text-center text-sm text-gray-600">
        ¿No tienes cuenta? <a href="register.php" class="font-semibold text-black hover:text-red-600">Regístrate</a>
    </div>
</div>
</div>
</main>
